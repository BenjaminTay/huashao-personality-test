# 本地查询/同步 GoatCounter 统计所需（.env.local 已 gitignore，不会提交）
# 真实做法：复制本文件为 .env.local，填入 token，仅保存在本地。

# GoatCounter 站点地址（无 /count 后缀）
GOATCOUNTER_SITE=https://benjaminlei.goatcounter.com

# 只读 API token：登录 https://benjaminlei.goatcounter.com 后右上菜单 → API 创建
GOATCOUNTER_API_TOKEN=

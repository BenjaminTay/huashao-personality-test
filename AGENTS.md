# 花学测试｜开发代理说明

## 项目目标

这是一个无需登录、移动端优先的娱乐人格测试网站。用户通过 24 道生活化的人际情境题，得到一个基于《花少 2》节目呈现与互联网“花学”共同形成的娱乐人格原型结果。

产品的核心体验是：

> 从轻松的生活问卷出发，逐幕进入关系暗流与修罗场，最后得到一份带档案感、网感和可分享性的“花学人格鉴定报告”。

七种结果不是对现实真人的心理诊断，也不是严谨的临床、人格或能力评估。所有“毛阿敏型、许晴型……”均应在产品中理解为节目/互联网文化原型。

## 开发原则

1. 内容层、计分层和视觉层分离。`data/` 是内容唯一来源，`lib/scoring.ts` 是计分唯一来源，UI 不复制题目、坐标或结果文案。
2. 底层模型要清楚，表层文案可以不正经。梗、黑话和戏剧化只负责体验，不得改变评分逻辑。
3. 一屏一题、单题推进、无需注册。选项点击后提供明确反馈，并在约 200–300ms 后自动进入下一题；用户始终可以返回修改。
4. 移动端优先，以约 390px 宽度为设计基准；桌面端是自然扩展，不得依赖 hover 才能理解或完成操作。
5. 结果必须可解释：展示六维分数和主型/副型/最不像的人格分析；不要把内部距离伪装成概率或“准确率”。（2026-09-03 v2.3 起经产品负责人批准，结果页不再强制展示“3 条真实选择证据”；scoring 层 topEvidenceQuestions 保留计算供回归，见 docs/decisions/ResultSystem-v2-梗感改版与QuestionSet-v3.2勘误.md §6）
6. 默认尊重用户隐私。答案优先保存在浏览器本地；若做匿名埋点，只记录必要事件，不上传原始答案、姓名、手机号或可识别个人信息。
7. 保持原创视觉。不得直接使用节目官方 Logo、海报、剧照、明星肖像或未确认授权的素材来模拟官方产品。

## 不可随意修改项

除非产品负责人明确批准并同步修改版本号，不得改变以下内容：

- 题目 ID、选项 ID、题目顺序、四幕分组和 `Question Set v3.2` 版本（矩阵 v3.1 修订见 docs/decisions/QuestionSet-v3.1-评分矩阵提案.md；v3.2 文案勘误见 docs/decisions/ResultSystem-v2-梗感改版与QuestionSet-v3.2勘误.md）；
- 24 道题的正文、96 个选项和新版 24×4 选项评分矩阵；
- V3 的六维定义（ID、语义与内部名）、七人格 v2.1 坐标（v2.1 修订见 docs/decisions/ArchetypeCoordinates-v2.1-提案.md）和 `Result System v2` 文案（六维展示文案与结果文案的 v2 梗感改版见 docs/decisions/ResultSystem-v2-梗感改版与QuestionSet-v3.2勘误.md，其中六维 ID、语义与内部名保持不变）；
- 六维 ID：R、S、B、D、G、I，以及它们的语义；
- 七个人格 ID、人格坐标和 `ArchetypeId`；
- 主型匹配必须使用 `Calibrated Item-Profile Matching v3.0`，不得把旧的简单六维欧氏距离作为最终分类器；
- 结果中的娱乐原型免责声明（2026-09-03 Result System v2.5 起，经产品负责人显式批准已从 UI 下线，不得擅自恢复；恢复需走同一审批流程。覆盖凭据见 docs/decisions/ResultSystem-v2-梗感改版与QuestionSet-v3.2勘误.md §8）；

如果确实需要改动，先修改对应数据文件和版本号，再让 UI、测试和 README 同步，不要在组件里做临时覆盖。

## V1 开发范围

### 必做

- 首页 Hero、玩法说明和开始按钮；
- 24 题单题流程，四幕过场和进度状态；
- 鉴定 Loading/Reveal；
- 六维结果面板；
- 主型、副型、最不像；
- “为什么是你”证据链（v2.3 起 UI 已下线，见开发原则 5 附注）；
- 七种固定结果文案、心眼子彩蛋和档案台词；
- 至少一种可下载/分享的结果卡，优先支持 3:4，同时为 9:16 留出接口；
- localStorage/sessionStorage 保存进行中的答案和结果；
- 移动端、桌面端、键盘操作、`prefers-reduced-motion` 和基本可访问性。

### 暂不做

登录、手机号/微信授权、用户中心、评论、排行榜、AI 生成分析、付费、好友系统、两人关系匹配、人格社区、复杂 CMS 和后台管理。

## 实现要求

- 使用 `QUESTIONS` 生成答题页，不手工复制题目内容。
- 使用 `DIMENSIONS` 生成六维图表和标签，不在组件里硬编码维度列表。
- 使用 `ARCHETYPES` 与 `RESULT_CONTENT` 生成结果页，不根据名字写分支逻辑。
- 所有计算入口从 `lib/scoring.ts` 导出；组件不自行计算分数。
- 未完成 24 题时可以展示临时进度，但不得输出正式主型结果；正式分类要求每题都有合法选项。
- 选项文本要保持清晰可读，避免在选项里额外加入“副作用”“这是高情商/低情商”等人格提示。
- 结果中的分数是展示分，不是百分比概率；不要显示“你有 73% 是某某型”或虚构统计置信度。
- 匿名埋点事件只在 `lib/analytics.ts` 定义：`start-test`、`complete-{type}`、`share-download`、`share-image`、`share-forward`、`share-copy`。只记录事件与主型，不携带原始答案；海报个性化姓名只存在本地，不随事件上传。
- 结果页与分享海报的称号、文案和 `heartEyeBalance` 一律来自 `data/`（海报的 `visualSymbol` 在 `data/archetypes.ts`），分享组件不维护自己的文案副本。
- `data/population-stats.ts` 是"测友坐标"快照，只能由 `.github/workflows/sync-population.yml` 从 GoatCounter 事件生成；展示口径为"人次"，样本小于 50 人次不显示百分比。

## 完成检查

交付前至少确认：

- `QUESTIONS.length === 24`，每题 4 个选项，共 96 个选项；
- 六维均有题目覆盖，所有评分都在 1–5；
- 七个理论原型用其最接近的选项路径作答时可以回到自身，作为模型一致性检查；
- 空答案、重复答案、非法选项、缺失题目会得到明确错误，而不是静默产生结果；
- 结果页没有心理诊断措辞、官方身份暗示或未授权素材；
- 390px 左右无横向滚动，按钮和选项可触摸，键盘焦点清晰；
- 减少动效设置下仍然能完整使用；
- 只做本地开发、验证和构建，不默认执行 commit、push、部署或外部发布。
